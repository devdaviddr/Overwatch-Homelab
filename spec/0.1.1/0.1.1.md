# Overwatch Homelab — Product Specification v0.1.1

## Overview

v0.1.1 is a minor UX and app configuration release focused on four coordinated changes:

1. Collapsible sidebar with a thin variant (icon-only) for space-efficient navigation.
2. Make the resource hero icon visually more square and consistent across list and detail views.
3. Render resource label(s) on the same line as the description in the resource header/card.
4. Include agent configuration fields as part of the **New Resource** wizard so operator onboarding is a single flow.

This release is intentionally small and scoped so the UX changes are well-specified and reversible.

---

## Goals

- Provide a compact sidebar mode so power users can reclaim screen real estate while keeping navigation discoverable.
- Improve visual consistency by standardising icon aspect ratio to a square container.
- Reduce friction: labels should not wrap to a separate line when space permits; group descriptive metadata inline.
- Streamline onboarding: allow creating a resource and configuring the agent (HUB_URL, heartbeat, metrics interval) in one wizard.

---

## UX Details & Acceptance Criteria

### 1) Collapsible Sidebar (Expanded <-> Thin)

Behavior:
- Sidebar has two modes: Expanded and Thin (collapsed).
- Expanded width: 240px (default). Thin width: 72px (icon-only).
- Toggle control: a chevron button at the top of the sidebar toggles modes and uses `aria-expanded`.
- Hovering a sidebar item in Thin mode shows a tooltip with Resource name + type + up to 2 labels.
- Collapse state persists per-browser using `localStorage` key: `ui.sidebarCollapsed` (value `"true"|"false"`).
- Transition: smooth width transition (`transition-width 160ms ease-out`) and fade/translate for label text.

Visuals:
- In Thin mode, only the heroicon and an optional small status dot (online/offline) are visible; text labels are hidden.
- Provide a small expand affordance (chevron) visible in both states.

Accessibility:
- Toggle button: role="button" aria-pressed, aria-expanded
- Tooltip uses role="tooltip" and appears on focus as well as hover.

Acceptance criteria:
- Toggling persists on page reload.
- Keyboard users can toggle with Enter/Space and traverse items with Tab.
- Tooltips show on focus and hover.

Files to change:
- `apps/hub-client/src/components/Sidebar.tsx`
- `apps/hub-client/src/components/SidebarItem.tsx` (or ResourceListItem)
- Tailwind utilities or a small CSS module for width transition


### 2) Square hero icon

Behavior:
- All hero icons use a square container (recommended: 40x40 px) with `rounded-md` border radius.
- Icon container should center the heroicon and preserve consistent padding; use `flex items-center justify-center`.
- On active/hover state, show a subtle ring using brand colour and/or fill.

Suggested Tailwind classes:
- `w-10 h-10 rounded-md flex items-center justify-center border-2 border-transparent` (default)
- Active: `border-brand-400 bg-brand-950` (or `bg-brand-500/10` depending on theme)

Acceptance criteria:
- Visuals for heroicon in Overview, Sidebar, and Detail pages match and are square.

Files to change:
- `apps/hub-client/src/components/ResourceCard.tsx`
- `apps/hub-client/src/pages/HomeLabPage.tsx` (header icon)
- `apps/hub-client/src/components/SidebarItem.tsx`


### 3) Label inline with description

Behavior:
- Resource header (and small card views) show: Name (bold) on first line, then Description *followed by* label chips on the same line (if width permits).
- Layout: second line is `display: flex; gap: 0.5rem; align-items: center;` with `flex-wrap: wrap;` so long descriptions wrap but label chips remain adjacent to the end of the visible description when possible.

Edge cases:
- If the description is empty, labels should remain visible on second line (aligned left).

Acceptance criteria:
- Label chips appear on same line as description for standard content lengths and wrap gracefully at narrow widths.

Files to change:
- `apps/hub-client/src/components/ResourceCard.tsx`
- `apps/hub-client/src/pages/OverviewPage.tsx` (card list)
- `apps/hub-client/src/pages/HomeLabPage.tsx` (detail header)


### 4) Agent configuration inside New Resource wizard

Behavior:
- Extend the existing 3-step wizard to include Agent Configuration as one of the steps.
- Recommended step ordering:
  1. Basic Info (name, description)
  2. Type & Labels
  3. Agent Configuration (HUB_URL, HEARTBEAT_INTERVAL_MS, METRICS_INTERVAL_MS, optional: `Run in Docker` toggle + platform hint)
  4. Review & Create

Fields & validation:
- `agentHubUrl` — optional, must be a valid URL if provided (default `http://localhost:3002` in example snippets)
- `heartbeatIntervalMs` — integer >= 1000 (default 15000)
- `metricsIntervalMs` — integer >= 5000 (default 60000)
- `runInDocker` — boolean (informational only; does not change DB)

UI bits:
- Provide an inline `.env` snippet preview in the Agent Configuration step that updates live as fields change. Include a copy-to-clipboard button.
- Display a platform-specific warning: "On macOS, Docker reports VM resources; run agent natively for accurate host metrics." (as a subdued hint)

API / Data model:
- `POST /api/homelabs` (create resource) must accept the new optional agent fields: `agentHubUrl`, `heartbeatIntervalMs`, `metricsIntervalMs`.
- `packages/shared-types` `CreateHomeLabSchema` should be updated to accept these optional fields.
- No DB migration required — columns already exist on `homelabs` (`agentHubUrl`, `heartbeatIntervalMs`, `metricsIntervalMs`).

Acceptance criteria:
- Creating a resource with agent fields persists those fields and the returned resource shows them in the Configuration tab.
- The `.env` snippet in the wizard is accurate and copyable.

Files to change:
- `apps/hub-client/src/components/CreateHomeLabWizard.tsx`
- `apps/hub-client/src/hooks/useHomeLabMutations.ts`
- `packages/shared-types/src/models.ts` (CreateHomeLabSchema)
- `apps/hub-server/src/routes/homelabs.ts` (create route validation)

---

## Implementation Tasks (developer checklist)

1. Create branch `release/0.1.1` (done in this branch) and commit spec (`spec/0.1.1/0.1.1.md`).
2. Update `shared-types` CreateHomeLabSchema to include optional agent fields (zod types + defaults).
3. Update `hub-server` create route to accept and persist agent fields.
4. Update `hub-client`:
   - Sidebar: add collapsed state, toggle button, animations, tooltips.
   - SidebarItem / Resource list: render icon-only in thin mode; show status dot.
   - ResourceCard / HomeLabPage header: square heroicon and inline label with description.
   - CreateHomeLabWizard: add Agent Configuration step and `.env` preview.
5. Unit / integration tests: update or add tests for CreateHomeLabSchema, hub-server route, and client mutation.
6. Manual QA checklist (see below).
7. Commit & push branch; open PR (if you want `git push origin release/0.1.1` — not performed by spec creation step unless requested).

---

## QA Checklist (manual)

- [ ] Sidebar toggle persists after refresh.
- [ ] Thin sidebar shows only heroicon; hover shows tooltip with name + type + labels.
- [ ] Heroicon appears square and consistent in Overview/Sidebar/Detail pages.
- [ ] On Resource cards and detail header, labels appear inline with description and wrap gracefully.
- [ ] New Resource wizard contains Agent Configuration step with `.env` preview and copy button.
- [ ] Creating a resource with agent fields persists values and Configuration tab shows them.
- [ ] Keyboard accessibility: toggle button is reachable and keyboard operable.

---

## Rollback & Compatibility

- All changes are additive: existing resources without agent fields continue to work.
- If the sidebar changes cause regressions, feature toggle via a small runtime flag (e.g., `UI_SIDEBAR_V2=false`) can be used to revert quickly.

---

## Notes / Design Tokens

- Collapsed width: `72px` (Tailwind: add utility or use inline style)
- Expanded width: `240px` (`w-60`)
- Heroicon container: `w-10 h-10 rounded-md` (40x40 px)
- Transition timing: 160ms ease-out

---

If this spec looks correct, confirm and implementation work will begin. If you'd like adjustments (e.g., smaller collapsed width, different step order for the wizard, or persistence to backend instead of localStorage), specify them and the spec will be updated.
